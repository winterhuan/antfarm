{
  "name": "antfarm",
  "version": "0.5.1",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "bin": {
    "antfarm": "dist/cli/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && cp src/server/index.html dist/server/index.html && chmod +x dist/cli/cli.js && node scripts/inject-version.js",
    "test": "npx tsx --test src/**/*.test.ts tests/**/*.test.ts",
    "test:unit": "npx tsx --test src/**/*.test.ts",
    "test:integration": "npx tsx --test tests/**/*.test.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "typecheck:test": "tsc --noEmit -p tsconfig.test.json",
    "start": "node dist/cli/cli.js"
  },
  "dependencies": {
    "json5": "^2.2.3",
    "yaml": "^2.4.5"
  },
  "devDependencies": {
    "@types/node": "^25.2.1",
    "typescript": "^5.9.3"
  }
}
